type Props = {
  params: {
    code: string;
  };
};

const OrderDetailPage = async ({ params }: Props) => {
  return (
    <div>
      <h1>Order Detail</h1>
    </div>
  );
};

export default OrderDetailPage;
